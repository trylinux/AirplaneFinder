# Production runtime dependencies.
#
# Install on the server with:
#   .venv/bin/pip install -r requirements-prod.txt
#
# This pulls in requirements.txt and adds the WSGI server. gunicorn lives
# here rather than in requirements.txt because it has no usable Windows
# support, and requirements.txt has to stay installable on every dev
# machine. Keeping it out of requirements.txt entirely, though, is how the
# systemd unit ended up pointing at a .venv/bin/gunicorn shim whose package
# wasn't installed — so it is pinned here instead of installed ad hoc.

-r requirements.txt

gunicorn==23.0.0
